sudo: required
services:
  - docker
language: python
python:
  - "2.7"
install:
  - pip install -r requirements.txt

script:
  - flake8 --ignore=E501 $(find . -name *.py)  # E501 = line length > 79
  - py.test -vv
